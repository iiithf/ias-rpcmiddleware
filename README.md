The test can be run using `RUNME.cmd`.
This middleware accepts C-like header files for service/client stub generation.

```bash
python middleware.py --help
python client_gen.py --help
python service_gen.py --help
```
